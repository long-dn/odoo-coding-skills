# Generic distribution — works with any LLM

Paste `odoo19-syntax.md` into ChatGPT custom instructions, Gemini system prompt, an LLM API `system` parameter, or any agent rules field.

Use `per-domain/` files individually if your target has a system-prompt size limit.
